---
kind: pipeline
name: default

steps:
- name: package
  image: harbor.dsrd.libraries.psu.edu/public/helm-push
  environment:
    HELM_PASSWORD:
      from_secret: HELM_PASSWORD
  commands:
  - helm package . --version $DRONE_TAG
  - helm push --username='robot$drone' --password=$HELM_PASSWORD solr-$DRONE_TAG.tgz https://harbor.dsrd.libraries.psu.edu/chartrepo/public
  when:
    event: [ tag ]
---
kind: signature
hmac: cf825c0bd4afb9b43e94669df821e89a7304a7d4a9d0f7790ab7986cde04a347

...
